import requests
import pandas
import dotenv
from importlib.metadata import version


print(f"requests {version('requests')} — Makes HTTP calls to APIs and web services")
print(f"pandas {version('pandas')} — Loads, cleans and analyses structured data")
print(f"python-dotenv {version('python-dotenv')} — Loads environment variables from a .env file")
